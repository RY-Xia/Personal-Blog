spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/blog?allowPublicKeyRetrieval=true&useSSL=FALSE&serverTimezone=UTC
    username: root
    password: popnono
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    root: warn
    com.runyao.myblog: info
  file:
    path: log/blog-pro.log
server:
  port: 8081
